{% trans_default_domain 'templatesTranslations' %}

<table class="table table-striped table-condensed">
    <tbody>
        {% for g in games %}
            <tr>
                {% set img = null %}
                {% if g.image_game is not null %}
                    {% set img = g.image_game %}
                {% endif %}
                <td class="home-last-adds-photo hidden-xs hidden-sm" data-toggle="tooltip" title="{{ g.name }} ({{ g.plateform }}) : {{ g.price }}€">
                    {% if img is not null %}
                        <a id="game-{{ g.id }}" href="#" onclick="return false;" data-html="true" data-trigger="focus" data-placement="left" data-toggle="popover" title="{{ g.name }} ({{ g.plateform }})" data-content="<img class='margin-auto' src='pictures/{{ g.game_id }}/{{ img }}' alt='Game picture' />"> 
                            <span class="glyphicon glyphicon-picture" aria-hidden="true"></span>
                        </a>
                    {% endif %}
                </td>
                <td class="home-last-adds-name" data-toggle="tooltip" title="{{ g.name }} ({{ g.plateform }}) : {{ g.price }}€">
                    {{ g.name }}
                </td>
                <td class="home-last-adds-plateform" data-toggle="tooltip" title="{{ g.name }} ({{ g.plateform }}) : {{ g.price }}€">
                    {{ g.plateform }}
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<a class="btn btn-primary margin-20" href="#">{{ 'home.last_adds.games.button'|trans }}</a>